#include <iostream>
#include <vector>

using namespace std;

#define sz 10

extern void BubbleSort(vector<int>& v);

int main() {
	vector<int> v(sz);
	for(int i = 0; i < sz; i++)
		v[i] = rand() % 100 + 1;
	BubbleSort(v);
	for(int i = 0; i < sz; i++)
		cout << v[i] << ' ';
	return 0;
}  
